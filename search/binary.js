// Array Binary Search

// const arr = [1, 2, 12, 25, 34, 57, 59, 91, 101, 111, 125, 135];

// const target = 25;

// function binarySearch(arr, target) {
//   let left = 0;
//   let right = arr.length - 1;

//   while (left <= right) {
//     let mid = Math.floor((left + right) / 2);

//     if (target === arr[mid]) {
//       return mid;
//     } else {
//       if (target < arr[mid]) {
//         right = mid - 1;
//       } else if (target > arr[mid]) {
//         left = mid + 1;
//       }
//     }
//   }

//   return -1;
// }

// console.log(targetIndex);
