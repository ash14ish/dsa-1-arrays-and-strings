// Longest common prefix

// let strs = ["flower", "flow", "flight", "fling"];

// let lcp = strs[0];
// let i;

// for (i = 1; i < strs.length; i++) {
//   let j = 0;

//   while (j < lcp.length && j < strs[i].length) {
//     if (lcp[j] !== strs[i][j]) break;
//     j++;
//   }

//   lcp = lcp.slice(0, j);

//   if (lcp === "") break;
// }

// console.log(lcp);

// Longest common prefix

// let strs = ["flower", "flow", "flora", "flight"];

// let lcp = strs[0];
// let i;

// for (i = 1; i < strs.length; i++) {
//   let j = 0;
//   while (j < lcp.length && j < strs[i].length) {
//     if (lcp[j] !== strs[i][j]) break;
//     j++;
//   }

//   lcp = lcp.slice(0, j);

//   if (lcp === "") break;
// }

// console.log(lcp);
